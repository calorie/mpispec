#!/bin/sh

# Time-stamp: <2007-11-14 22:32:13 shinya>

INSTDIR=@prefix@
CC="mpicc"
RUN="mpirun"
EXEC="exec_test"
PROCLOGFLAGS="-I${INSTDIR}/include"
PROCLOGLIBS="-L${INSTDIR}/lib -ldatalog -lproclog -lm"

$CC $PROCLOGFLAGS $PROCLOGLIBS src/*.c test/*.c -o $EXEC
$RUN $* ./$EXEC
rm -f *.o $EXEC
