#!/bin/sh

# Time-stamp: <2007-11-14 22:32:17 shinya>

INSTDIR=@prefix@
CC="mpicc"
EXEC="exec_test"
PROCUTFLAGS="-I${INSTDIR}/include"
PROCUTLIBS="-L${INSTDIR}/lib -ldatareplay -lprocut -lm"

$CC $PROCUTFLAGS $PROCUTLIBS src/*.c test/*.c -o $EXEC
./$EXEC $*
rm -f *.o $EXEC
