#!/bin/sh

INSTDIR=@prefix@
CC="mpicc"
EXEC="exec_test"
MPISPECFLAGS="-I${INSTDIR}/include -finstrument-functions -rdynamic"
MPISPECLIBS="-L${INSTDIR}/lib -lmpispec -lm"

$CC $MPISPECFLAGS $MPISPECLIBS src/*.c spec/*.c -o $EXEC
mpirun $* ./$EXEC
rm -f *.o $EXEC
