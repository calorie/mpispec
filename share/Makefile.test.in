
INSTDIR = @prefix@
CC=mpicc
MPISPECFLAGS = -I${INSTDIR}/include
MPISPECLIBS = -L${INSTDIR}/lib -lmpispec -lm
RUNTEST = run-test

mpispec:
	@${CC} ${MPISPECFLAGS} ${MPISPECLIBS} ${SRC} ${SPECRC} -o ${RUNTEST}
	@mpirun ${ARGV} ./${RUNTEST}
	@rm -f ${RUNTEST} *.o *.so *~
